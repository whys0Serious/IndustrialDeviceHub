<UserControl x:Class="DeviceHub.Client.Views.DeviceListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DeviceHub.Client.Views"
             mc:Ignorable="d"
             xmlns:hc="https://handyorg.github.io/handycontrol">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!-- 标题 -->
        <TextBlock Grid.Row="0" Text="设备列表" FontSize="24" FontWeight="Bold" Margin="0,0,0,15"/>

        <!-- 工具栏 -->
        <Border Grid.Row="1" Background="#F5F5F5" Padding="10" CornerRadius="4" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="关键字：" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}"
                      Width="200" Height="30" VerticalContentAlignment="Center" Margin="0,0,15,0"/>

                <TextBlock Text="状态：" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox ItemsSource="{Binding StatusOptions}"
                       SelectedItem="{Binding SelectedStatus}"
                       DisplayMemberPath="Display"
                       Width="120" Height="30" Margin="0,0,15,0"/>

                <TextBlock Text="分类：" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox ItemsSource="{Binding CategoryOptions}"
                       SelectedItem="{Binding SelectedCategory}"
                       DisplayMemberPath="Name"
                       Width="150" Height="30" Margin="0,0,15,0"/>

                <Button Content="搜索" Command="{Binding SearchCommand}"
                     Width="80" Height="30" Margin="0,0,5,0"/>
                <Button Content="重置" Command="{Binding ResetCommand}"
                     Width="80" Height="30"/>
            </StackPanel>
        </Border>

        <!-- 数据区：加载中 / 空 / DataGrid -->
        <Grid Grid.Row="2">
            <!-- 加载中 -->
            <TextBlock Text="加载中..."
                    HorizontalAlignment="Center" VerticalAlignment="Center"
                    FontSize="16" Foreground="Gray"
                    Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}"/>

            <!-- 空数据 -->
            <TextBlock Text="暂无数据"
                    HorizontalAlignment="Center" VerticalAlignment="Center"
                    FontSize="16" Foreground="Gray">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsLoading}" Value="False"/>
                                    <Condition Binding="{Binding HasData}" Value="False"/>
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Visibility" Value="Visible"/>
                            </MultiDataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <!-- DataGrid -->
            <DataGrid ItemsSource="{Binding Devices}"
                   AutoGenerateColumns="False"
                   IsReadOnly="True"
                   CanUserAddRows="False"
                   HeadersVisibility="Column"
                   GridLinesVisibility="Horizontal"
                   RowHeight="35">
                <DataGrid.Style>
                    <Style TargetType="DataGrid">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasData}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Style>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="60"/>
                    <DataGridTextColumn Header="设备名称" Binding="{Binding Name}" Width="200"/>
                    <DataGridTextColumn Header="设备编号" Binding="{Binding Code}" Width="150"/>
                    <DataGridTextColumn Header="状态" Binding="{Binding StatusText}" Width="100"/>
                    <DataGridTextColumn Header="分类" Binding="{Binding CategoryName}" Width="150"/>
                    <DataGridTextColumn Header="备注" Binding="{Binding Remark}" Width="*"/>
                    <DataGridTextColumn Header="创建时间" Binding="{Binding CreatedAt, StringFormat=yyyy-MM-dd HH:mm}" Width="150"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- 底部：左统计 + 右分页 -->
        <Border Grid.Row="3" Margin="0,15,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 左：状态统计 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock VerticalAlignment="Center">
            <Run Text="总数：" Foreground="#34495E" FontWeight="Bold"/>
            <Run Text="{Binding TotalDevices, Mode=OneWay}" FontWeight="Bold"/>
                    </TextBlock>

                    <TextBlock Margin="15,0,0,0" VerticalAlignment="Center">
            <Run Text="运行中：" Foreground="#27AE60"/>
            <Run Text="{Binding RunningCount, Mode=OneWay}" Foreground="#27AE60" FontWeight="Bold"/>
                    </TextBlock>

                    <TextBlock Margin="15,0,0,0" VerticalAlignment="Center">
            <Run Text="报警：" Foreground="#E74C3C"/>
            <Run Text="{Binding AlarmCount, Mode=OneWay}" Foreground="#E74C3C" FontWeight="Bold"/>
                    </TextBlock>

                    <TextBlock Margin="15,0,0,0" VerticalAlignment="Center">
            <Run Text="停止：" Foreground="#95A5A6"/>
            <Run Text="{Binding StoppedCount, Mode=OneWay}" Foreground="#95A5A6" FontWeight="Bold"/>
                    </TextBlock>
                </StackPanel>

                <!-- 中：错误提示 -->
                <TextBlock Grid.Column="1"
               Text="{Binding ErrorMessage}"
               Foreground="Red" VerticalAlignment="Center"
               HorizontalAlignment="Center"/>

                <!-- 右：分页 -->
                <hc:Pagination Grid.Column="2"
                   HorizontalAlignment="Right"
                   VerticalAlignment="Center"
                   MaxPageCount="{Binding TotalPages}"
                   PageIndex="{Binding CurrentPageIndex, Mode=TwoWay}"
                   DataCountPerPage="{Binding PageSize}"
                   IsJumpEnabled="True">
                    <hc:Interaction.Triggers>
                        <hc:EventTrigger EventName="PageUpdated">
                            <hc:EventToCommand Command="{Binding PageUpdatedCommand}"/>
                        </hc:EventTrigger>
                    </hc:Interaction.Triggers>
                </hc:Pagination>
            </Grid>
        </Border>
    </Grid>
</UserControl>
