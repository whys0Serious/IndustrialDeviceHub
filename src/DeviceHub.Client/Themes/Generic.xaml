<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:DeviceHub.Client.Controls">
    
    <Style TargetType="{x:Type controls:CustomWindow}">
        <Setter Property="Background"   Value="{DynamicResource WindowBackgroundBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:CustomWindow}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="32"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- 自动挂标题栏 -->
                            <controls:TitleBarControl Grid.Row="0"
                                                      WindowTitle="{Binding Title, RelativeSource={RelativeSource TemplatedParent}}"
                                                      ShowMinimize="{Binding ShowMinimize, RelativeSource={RelativeSource TemplatedParent}}"
                                                      ShowMaximize="{Binding ShowMaximize, RelativeSource={RelativeSource TemplatedParent}}"
                                                      ShowClose="{Binding ShowClose, RelativeSource={RelativeSource TemplatedParent}}"/>

                            <!-- 窗口内容 -->
                            <ContentPresenter Grid.Row="1"/>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>